import CoursesDashboard from '@/components/user/(student)/user.course';
import UserProfiles from '@/components/user/user.profile';
import React from 'react';

const EnrollCourse: React.FC = () => {
    return (
        <CoursesDashboard />
    );
};

export default EnrollCourse;